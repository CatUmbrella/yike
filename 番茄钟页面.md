# 番茄钟页面

## 一.总述

包含1个主页，2个子页面，5个弹窗页面

## 二.主页

即点击导航栏进入的页面

有三个组件 最上方为 ***历史记录*** 组件 该板块放置使用过番茄钟的已完成或者未完成事件 该板块偏小 视觉权重低 这个组件仅展示最近完成的事件（两到三件 事件标题用summary字段显示 标题右方为番茄 最多显示三个番茄 超过三个番茄显示为 番茄× 多少 标题下方显示完成时间 用completed_at字段应该是可行的 还要根据completed_at字段是否为空显示已完成未完成） 如果是使用过番茄钟但是未完成的事件 可以考虑使用updated_at字段 这个只是建议 该组件右上角有一个 “查看全部>”  灰色 点击跳转至子页面 ***历史记录详细页***

中间为 ***事件箱*** 组件 该板块可以直接复用安排页的设计 但是不复用四象限的上色逻辑 但是左侧的复选框要做成可交互的 选中事件之后 打勾 然后事件背景变为深蓝色（ 事件胶囊颜色默认为浅蓝色 和安排页面的事件箱中胶囊颜色一致） 该板块稍大 视觉权重中上 事件箱组件不要复用安排页面“点击空白进入录入页面”的跳转逻辑

最下方为 ***开始番茄钟*** 组件 使用圆角矩形框 在没有选中事件之前 “开始番茄钟”这个按钮呈灰色 选中事件之后呈蓝色 点击即跳转至子页面 ***番茄钟计时页面*** 开始计时 该组件默认显示”选择一个任务“”点击选择事件箱中的任务开始专注“ 选择事件后显示为选中事件的标题 下方为 ”预计专注时间：“ 右方固定显示一个番茄的图标 该图标为自定义图标

该页面还需要一个从主页复用的添加按钮 交互逻辑一致

## 三.子页面

### 番茄钟计时页面

该页面为点击 开始番茄钟 进入的页面 如果在事件详细页点击开始番茄钟 同样会进入该页面

该页面有四个小组件

第一个组件为一个计时器 00：00：00 不需要矩形框 可以直接放在背景上 正在计时时下方小字显示“专注中” 暂停计时时显示“暂停中” 两种状态用不同的颜色进行区分

第二个组件为 ***打断***  使用圆角矩形 左侧为垂直排列的”+打断“ 点击后弹出一个小弹窗 该小弹窗作用为录入打断缘由 该弹窗暂时作为占位符 中间一条分割线 右侧为可以滑动的矩形框 显示具体打断缘由 后台记录具体时间和番茄钟进行了多少分钟产生的记录行为 如果该事件已经解决 则划一道横线 并且整体变灰 表示已经解决 未解决按照原色显示

第三个组件为 ***想法***  使用圆角矩形 左侧为垂直排列的”+想法“  点击后弹出一个小弹窗 该小弹窗作用为录入具体想法 该弹窗暂时作为占位符 中间一条分割线 右侧为可以滑动的矩形框 显示具体想法 后台记录具体时间和番茄钟进行了多少分钟产生的记录行为

第四个组件为 ***步骤***  使用圆角矩形 左侧为垂直排列的”步骤“  中间一条分割线 右侧为可以滑动的矩形框 显示具体步骤 左侧有一个复选框 勾引表示完成该步骤 划横线并变为灰色 后台记录具体耗时 如出现短时间内完成大量步骤的情况 则不进行计时 步骤内容可修改并且同步

最下方为长条胶囊按钮 点击可以暂停专注 下方有一个结束专注 点击弹出弹窗 询问你是否要结束

事件结束之后还会有一个弹窗 负责让用户决定产生的想法要不要录入事件箱 大致样式为

序号 想法内容作为标题

目的 可修改

步骤 可添加可编辑 预计耗时可编辑 交互逻辑复用之前的

最下方为是否加入事件箱

按顺序一个一个弹出卡片 选择完毕之后 正式结束番茄钟

### 历史记录具体页面

该页面为点击 ”历史记录>“进入的页面 这里面显示的是所有使用过番茄钟的事件 以事件卡片的形式展示 按时间分类 默认显示本月的事件 按照今天 昨天 之前的日期（仅x日 不写月份）这个顺序排列 点击最上面的”xxxx年x月 一个向下的箭头 “可以选择年份和日期 可能还要做搜索功能 支持对名称和日期的检索（看一下难度怎么样吧 太难就先不做）

事件卡片的样式为 事件标题 完成与未完成状态 下方是打断次数和想法 右边是番茄个数

点击事件卡片弹出一个大的卡片 上面记录了

”事件标题：（完成状态） 预计耗时“

”目的（若有则显示）“

”具体步骤 每一步的预计耗时（若有则显示）“ 步骤和预计耗时允许编辑 逻辑复用之前的 如果产生修改后台记录修改时间 首次内容和最后一次的内容 产生多次修改覆写最后一次的内容和修改时间

实际用时 番茄个数 （完成后显示）

打断 具体打断事由 时间 不支持编辑

想法 具体想法 时间 不支持编辑

最下面是一个黑色的叉 代表关闭该卡片
